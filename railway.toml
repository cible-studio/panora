[build]
builder = "nixpacks"

[deploy]
startCommand = "php artisan migrate --force && php-fpm & caddy run --config /etc/caddy/Caddyfile"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[healthcheck]
path = "/health"
timeout = 30